import { Card } from '@/components/ui';

export function Dashboard() {
  return (
    <div style={{ maxWidth: 800, margin: '0 auto', padding: '2rem' }}>
      <Card padding="lg">
        <h1>Dashboard</h1>
        <p>Área do usuário será implementada após autenticação.</p>
      </Card>
    </div>
  );
}
